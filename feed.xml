<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
    <title>willmafh</title>
    <link href="https://willmafh.github.io/feed.xml" rel="self" />
    <link href="https://willmafh.github.io" />
    <updated>2026-02-14T21:30:28+08:00</updated>
    <author>
        <name>willmafh</name>
    </author>
    <id>https://willmafh.github.io</id>

    <entry>
        <title>Linux tracing techs</title>
        <author>
            <name>willmafh</name>
        </author>
        <link href="https://willmafh.github.io/linux-tracing-techs.html"/>
        <id>https://willmafh.github.io/linux-tracing-techs.html</id>
            <category term="uprobes"/>
            <category term="tracing"/>
            <category term="tracepoints"/>
            <category term="perf"/>
            <category term="kprobes"/>
            <category term="eBPF"/>

        <updated>2026-01-28T21:35:15+08:00</updated>
            <summary type="html">
                <![CDATA[
                    kprobes brief history Linux v2.6.9 introduced kprobes into its mainline, but only for i386 v2.6.10 begins to add kprobes support for x86_64 Linux v2.6.12 allows multiple kprobes at the same address v2.6.13 starts to support kretprobe v2.6.14 groups kprobes related functions to section .kprobes.text, so&hellip;
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <h1 id="kprobes">kprobes</h1>
<h2 id="brief-history">brief history</h2>
<ul>
<li>Linux v2.6.9 introduced <code>kprobe</code>s into its mainline, but only for <code>i386</code></li>
<li>v2.6.10 begins to add kprobes support for <code>x86_64</code></li>
<li>Linux v2.6.12 allows multiple kprobes at the same address</li>
<li>v2.6.13 starts to support <code>kretprobe</code></li>
<li>v2.6.14 groups kprobes related functions to section <code>.kprobes.text</code>, so that kprobes to these codes will be rejected</li>
<li>v2.6.15 updates: using <code>percpu</code> infra to manage some internal variables; using <code>RCU(Read-Copy-Update) list</code> to manage kprobes; more checks on kprobes register, including kernel module address check</li>
<li>v2.6.16 updates: more kprobes rejects on kernel functions; refactor kprobes register functions to check whether we are probing a kernel module</li>
<li>v2.6.17 <code>i386</code> updates its <code>kretprobe_trampoline_holder</code>; updates <code>kprobe_fault_handler</code></li>
<li>v2.6.18 kprobes registers for <code>page fault notifications</code> when their is an active probe registered; <code>tcpprobe</code> module to probe <code>tcp_sendmsg</code></li>
<li>v2.6.19 add <code>symbol_name</code> and <code>offset</code> fields to <code>struct kprobe</code> so you can directly using function name to register kprobes; <code>dccp_probe</code> module to probe <code>dccp_sendmsg</code></li>
<li>v2.6.21 add basic <code>debugfs</code> support for kprobes, <code>/sys/kernel/debug/kprobes/list</code> lists all registered probes on the system</li>
<li>v2.6.22 more on <code>debugfs</code>, add <code>/sys/kernel/debug/kprobes/enabled</code> to globally turn registered kprobes on/off and the default value is on</li>
<li>v2.6.24 add basic <code>kretprobe blacklist</code> support</li>
<li>v2.6.25 kretprobe supports optional user-specified <code>entry_handler</code> which runs on function entry and also supports <code>private data</code> which can pass data between <code>entry_handler</code> and <code>ret_handler</code></li>
<li>v2.6.26 add basic <code>kprobe blacklist</code> support; batch registration/unregistration of a group of probes interfaces</li>
<li>v2.6.29 add <code>module notifier call back</code>, which will check kprobes on the module; add <code>flags</code> field to <code>struct kprobe</code> to mark kprobe gone <code>KPROBE_FLAG_GONE</code> and remove its instruction buffer</li>
<li>v2.6.30 enable/disable probes interfaces, if kp-&gt;flags is set <code>KPROBE_FLAG_DISABLED</code>, that kp will be registered but disabled, so, it’s handlers aren’t hit until calling enable_kprobe(kp)</li>
<li>v2.6.33 updates kprobe blacklist; check whether kprobe re-registered</li>
<li>v2.6.34 kprobe optimization <code>KPROBE_FLAG_OPTIMIZED</code> for <code>i386</code> and <code>x86_64</code>;  kprobes sysctl interface <code>/proc/sys/debug/kprobes-optimization</code> to control kprobe optimization</li>
<li>v2.6.39 do not optimize in the entry code due to the unstable stack handling</li>
</ul>
<h2 id="initialization">initialization</h2>
<ul>
<li>when Linux is starting, it will call <code>init_kprobes</code> in its process of initialization</li>
</ul>
<pre><code class="language-c">struct notifier_block *i386die_chain;

int register_die_notifier(struct notifier_block *nb)
{
    // ...
    err = notifier_chain_register(&amp;i386die_chain, nb);
    // ...
    return err;
}
</code></pre>
<pre><code class="language-c">static struct notifier_block kprobe_exceptions_nb = {
    .notifier_call = kprobe_exceptions_notify,
    .priority = 0x7fffffff /* we need to notified first */
};

static int __init init_kprobes(void)
{
    int i, err = 0;

    for (i = 0; i &lt; KPROBE_TABLE_SIZE; i++) {
        INIT_HLIST_HEAD(&amp;kprobe_table[i]);  // for kprobes
        INIT_HLIST_HEAD(&amp;kretprobe_inst_table[i]);  // for kretprobes
    }

    err = arch_init_kprobes();  // here to register a kprobe for trampoline
    if (!err)
        // register kprobe_exceptions_nb to i386die_chain
        err = register_die_notifier(&amp;kprobe_exceptions_nb);

    return err;
}

__initcall(init_kprobes);
</code></pre>
<h2 id="how-does-kprobe-work">how does kprobe work</h2>
<ul>
<li>helper functions</li>
</ul>
<pre><code class="language-c">/* Attach to insert probes on any functions which should be ignored */
#define __kprobes  __attribute__((__section__(&quot;.kprobes.text&quot;)))

void __kprobes arch_copy_kprobe(struct kprobe *p)
{
    memcpy(p-&gt;ainsn.insn, p-&gt;addr, MAX_INSN_SIZE * sizeof(kprobe_opcode_t));
    p-&gt;opcode = *p-&gt;addr;
}

void __kprobes arch_arm_kprobe(struct kprobe *p)
{
    *p-&gt;addr = BREAKPOINT_INSTRUCTION;
    // ...
}

static int __kprobes in_kprobes_functions(unsigned long addr)
{
    if (addr &gt;= (unsigned long)__kprobes_text_start
        &amp;&amp; addr &lt; (unsigned long)__kprobes_text_end)
        return -EINVAL;
    return 0;
}
</code></pre>
<ul>
<li>call <code>register_kprobe</code> to register a kprobe we want to probe</li>
</ul>
<pre><code class="language-c">int register_kprobe(struct kprobe *p)
{
    // ...
    if ((ret = in_kprobes_functions((unsigned long) p-&gt;addr)) != 0)
        return ret;

    // ...
    // NOTE: add new kprobe to corresponding hash table slot
    hlist_add_head(&amp;p-&gt;hlist,
               &amp;kprobe_table[hash_ptr(p-&gt;addr, KPROBE_HASH_BITS)]);
        
    arch_arm_kprobe(p);
    // ...
}
</code></pre>
<ul>
<li>when execution comes to the probing address, an <code>int3</code> happened and <code>do_int3</code> will be called then, and it will notify our register kprobes, from there, <code>pre_handler</code>/<code>break_handler</code> inside kprobe will be called</li>
</ul>
<pre><code class="language-asm">ENTRY(int3)
    // ...
    call do_int3  // here to call do_int3
    // ...
</code></pre>
<pre><code class="language-c">#ifdef CONFIG_KPROBES
asmlinkage int do_int3(struct pt_regs *regs, long error_code)
{
    // here to notify registered kprobes
    if (notify_die(DIE_INT3, &quot;int3&quot;, regs, error_code, 3, SIGTRAP)
            == NOTIFY_STOP)
        return 1;
    // back to normal ...
}
#endif
</code></pre>
<pre><code class="language-c">static inline int notify_die(enum die_val val,char *str,struct pt_regs *regs,long err,int trap, int sig)
{
    struct die_args args = { .regs=regs, .str=str, .err=err, .trapnr=trap,.signr=sig };
    // here kprobe_exceptions_notify inside kprobe_exceptions_nb will be called
    return notifier_call_chain(&amp;i386die_chain, val, &amp;args);
}
</code></pre>
<pre><code class="language-c">int kprobe_exceptions_notify(struct notifier_block *self, unsigned long val,
                 void *data)
{
    struct die_args *args = (struct die_args *)data;
    switch (val) {
    case DIE_INT3:
        // p-&gt;break_handler &amp; p-&gt;pre_handler
        if (kprobe_handler(args-&gt;regs))  // handler cb inside
            return NOTIFY_STOP;
        break;
        // ... p-&gt;post_handler
        // ... p-&gt;fault_handler
    }
    return NOTIFY_DONE;
}
</code></pre>
<pre><code class="language-c">static inline int kprobe_handler(struct pt_regs *regs)
{
    struct kprobe *p;
    u8 *addr = (u8 *) (regs-&gt;eip - 1);
    // ...
    p = get_kprobe(addr);
    // ...
    if (p-&gt;pre_handler(p, regs)) {  // NOTE: where magic happened
        /* handler has already set things up, so skip ss setup */
        return 1;
    }
    // ...
}
</code></pre>
<h2 id="how-does-kretprobe-work">how does kretprobe work</h2>
<ul>
<li>in order to support <code>kretprobe</code>, at boot time, <code>init_kprobes</code> will first register a kprobe at the <code>trampoline</code>, which is  an arbitrary piece of code – typically just a nop instruction</li>
</ul>
<pre><code class="language-c">void kretprobe_trampoline_holder(void)
{
asm volatile (  &quot;.global kretprobe_trampoline\n&quot;
        &quot;kretprobe_trampoline: \n&quot;
        &quot;nop\n&quot;);
}

static struct kprobe trampoline_p = {
    .addr = (kprobe_opcode_t *) &amp;kretprobe_trampoline,
    .pre_handler = trampoline_probe_handler
};

int __init arch_init_kprobes(void)
{
    return register_kprobe(&amp;trampoline_p);
}
</code></pre>
<ul>
<li>when you call <code>register_kretprobe</code>, kprobes establishes a kprobe at the entry to the function and when the probed function is called and this probe is hit, kprobes saves a copy of the return address, and replaces the return address with the address of a “trampoline.”</li>
</ul>
<pre><code class="language-c">void arch_prepare_kretprobe(struct kretprobe *rp, struct pt_regs *regs)
{
    unsigned long *sara = (unsigned long *)&amp;regs-&gt;esp;
    struct kretprobe_instance *ri;

    if ((ri = get_free_rp_inst(rp)) != NULL) {
        ri-&gt;rp = rp;
        ri-&gt;task = current;
        ri-&gt;ret_addr = (kprobe_opcode_t *) *sara;  // important here! save original fn return addr

        // replace the return addr with trampoline addr
        *sara = (unsigned long) &amp;kretprobe_trampoline;

        // ...
    }
    // ...
}

static int pre_handler_kretprobe(struct kprobe *p, struct pt_regs *regs)
{
    struct kretprobe *rp = container_of(p, struct kretprobe, kp);

    /*TODO: consider to only swap the RA after the last pre_handler fired */
    arch_prepare_kretprobe(rp, regs);
    return 0;
}

int register_kretprobe(struct kretprobe *rp)
{
    // ...
    rp-&gt;kp.pre_handler = pre_handler_kretprobe;

    /* Establish function entry probe point */
    if ((ret = register_kprobe(&amp;rp-&gt;kp)) != 0) {
    }
    // ...
}
</code></pre>
<ul>
<li>so that when function return, control passes to the trampoline, which is already been registered as a kprobe in <code>init_kprobes</code>, and that probe is hit, so its pre_handler <code>trampoline_probe_handler</code> will be called</li>
<li>after calling the user-specified handler associated with the kretprobe, the original function return address will be restored, and execution will be resumed</li>
</ul>
<pre><code class="language-c">int trampoline_probe_handler(struct kprobe *p, struct pt_regs *regs)
{
    // ...
    head = kretprobe_inst_table_head(current);

    hlist_for_each_entry_safe(ri, node, tmp, head, hlist) {
        if (ri-&gt;task != current)
            continue;

        if (ri-&gt;rp &amp;&amp; ri-&gt;rp-&gt;handler)
            ri-&gt;rp-&gt;handler(ri, regs);  // called user-specified handler

        orig_ret_address = (unsigned long)ri-&gt;ret_addr;
        // ...
    }

    regs-&gt;eip = orig_ret_address;  // restore the original return addr
    // ...
}
</code></pre>
<h2 id="debugfs-interface-for-kprobes">debugfs interface for kprobes</h2>
<ul>
<li>to list all registered probes on the system</li>
</ul>
<pre><code class="language-bash">cat /sys/kernel/debug/kprobes/list
</code></pre>
<ul>
<li>to globally turn registered kprobes on or off</li>
</ul>
<pre><code class="language-bash">cat /sys/kernel/debug/kprobes/enabled
# turn on
echo 1 &gt; /sys/kernel/debug/kprobes/enabled
# turn off
echo 0 &gt; /sys/kernel/debug/kprobes/enabled
</code></pre>
<h2 id="kprobes-sysctl-interface">kprobes sysctl interface</h2>
<ul>
<li>check kprobes optimization status</li>
</ul>
<pre><code class="language-bash">cat /proc/sys/debug/kprobes-optimization
</code></pre>
<ul>
<li>turn on or off kprobes optimization</li>
</ul>
<pre><code class="language-bash"># turn off
echo 0 &gt; /proc/sys/debug/kprobes-optimization
# turn on
echo 1 &gt; /proc/sys/debug/kprobes-optimization
</code></pre>
<h1 id="uprobes">uprobes</h1>
<h2 id="brief-history-1">brief history</h2>
<ul>
<li>Linux v3.5 introduced <code>uprobe</code> into its mainline</li>
</ul>
<h1 id="tracepoints">tracepoints</h1>
<ul>
<li>NOTE: tracepoints implementation based on Linux v2.6.28 codes reading, may update it after reading new version of it</li>
</ul>
<h2 id="brief-history-2">brief history</h2>
<ul>
<li>Linux v2.6.28 introduced <code>tracepoint</code> into its mainline</li>
<li>v2.6.29 splits original <code>DEFINE_TRACE</code> into <code>DECLARE_TRACE</code> and <code>DEFINE_TRACE</code>; if the tracepoint has to be used in kernel modules, an <code>EXPORT_TRACEPOINT_SYMBOL_GPL</code> or <code>EXPORT_TRACEPOINT_SYMBOL</code> can be used to export the defined tracepoints</li>
<li>v2.6.30 the macro <code>TRACE_EVENT</code> was introduced, which is far more powerful because it automates the “boilerplate” code needed to bridge the gap between a kernel function and a monitoring tool like <code>perf</code> or <code>ftrace</code></li>
<li>v2.6.31 all <code>predefined</code> tracepoint events were grouped under <code>include/trace/events</code> by organizing events into subsystems, and introduced <code>/sys/kernel/debug/tracing/events</code>  interface, giving each event its own directory with <code>enable</code> file</li>
<li>v2.6.32 add <code>regfunc</code> and <code>unregfunc</code> fields to <code>struct tracepoint</code> for more flexible functionality</li>
<li>v2.6.33 more macros <code>DECLARE_EVENT_CLASS</code> <code>DEFINE_EVENT</code> <code>DEFINE_EVENT_PRINT</code> to facilitate tracepoint usage for other tools</li>
<li>v2.6.35 update <code>struct tracepoint</code> and change related macros internal</li>
<li>v2.6.37 using <code>JUMP_LABEL</code> macro to test whether a tracepoint is enabled or not, and its related stuff</li>
<li>v2.6.38 using <code>__tracepoints_ptrs</code> section for iteration on the tracepoints; macro <code>DECLARE_TRACE_CONDITION</code></li>
<li>v3.0 performance optimization using asm <code>goto __jump_table</code></li>
</ul>
<h2 id="how-does-tracepoint-work">how does tracepoint work</h2>
<ul>
<li>using <code>DEFINE_TRACE</code>, <code>TPPROTO</code> and <code>TPARGS</code> to define a tracepoint</li>
<li>the tracepoint name will be put in <code>__tracepoints_strings</code> section, and the tracepoint itself will be put in <code>__tracepoints</code> section</li>
</ul>
<pre><code class="language-c">DEFINE_TRACE(sched_wakeup_new,
    TPPROTO(struct rq *rq, struct task_struct *p),
    TPARGS(rq, p));

# the above example will expand to the following codes

# this is the tracepoint function called in other important places
static inline void trace_sched_wakeup_new(struct rq *rq,
                                          struct task_struct *p) {

  static const char __tpstrtab_sched_wakeup_new[]
      __attribute__((section(&quot;__tracepoints_strings&quot;))) =
          &quot;sched_wakeup_new:TPPROTO(struct rq *rq, struct task_struct *p)&quot;;

  static struct tracepoint __tracepoint_sched_wakeup_new
      __attribute__((section(&quot;__tracepoints&quot;), aligned(8))) = {
          __tpstrtab_sched_wakeup_new,
                  0,
                  NULL
          };
  // when tracepoint does not activated, then just skip it
  if (unlikely(__tracepoint_sched_wakeup_new.state))
    do {
      void **it_func;
      rcu_read_lock_sched();
      it_func = rcu_dereference((&amp;__tracepoint_sched_wakeup_new)-&gt;funcs);
      if (it_func) {
        do {
          // call tracepoint probe funcs here!!
          ((void (*)(struct rq * rq, struct task_struct * p))(*it_func))(rq, p);
        } while (*(++it_func));
      }
      rcu_read_unlock_sched();
    } while (0);
}
</code></pre>
<ul>
<li>when you want to register a probe function to some tracepoint, just define the probe function according to the signature of proto of tracepoint definition, then call the generated register function, which will then call <code>tracepoint_probe_register</code> to connect a probe to a tracepoint</li>
</ul>
<pre><code class="language-c">static void set_tracepoint(struct tracepoint_entry **entry,
    struct tracepoint *elem, int active)
{
    // ...
    // assign funcs in tracepoint entry to tracepoint
    rcu_assign_pointer(elem-&gt;funcs, (*entry)-&gt;funcs);
    elem-&gt;state = active;  // then activate the tracepoint
}

void tracepoint_update_probe_range(struct tracepoint *begin,
    struct tracepoint *end)
{
    // ...
    for (iter = begin; iter &lt; end; iter++) {
        mark_entry = get_tracepoint(iter-&gt;name);
        if (mark_entry) {
            set_tracepoint(&amp;mark_entry, iter,
                    !!mark_entry-&gt;refcount);
        } // ...
    }
    // ...
}

static void tracepoint_update_probes(void)
{
    /* Core kernel tracepoints */
    tracepoint_update_probe_range(__start___tracepoints,
        __stop___tracepoints);
    /* tracepoints in modules. */
    module_update_tracepoints();
}

int tracepoint_probe_register(const char *name, void *probe)
{
    // ...
    // add probe to corresponding tracepoint entry
    old = tracepoint_entry_add_probe(entry, probe);
    // ...
    // move probe funcs in tracepoint entry to corresponding tracepoint
    tracepoint_update_probes();
    // ...
}
</code></pre>
<pre><code class="language-c">static inline int register_trace_sched_wakeup_new(void (*probe)(struct rq *rq, struct task_struct *p)) {
  return tracepoint_probe_register(&quot;sched_wakeup_new:TPPROTO(struct rq *rq, struct task_struct *p)&quot;, (void *)probe);
}
</code></pre>
<pre><code class="language-c">struct tracepoint {
  const char *name;
  int state;
  void **funcs;  // all registered probe functions for this tracepoint
} __attribute__((aligned(8)));
</code></pre>
<ul>
<li>tracepoint actual calling example</li>
</ul>
<pre><code class="language-c"># sys_fork will call do_fork, and it will call wake_up_new_task
void wake_up_new_task(struct task_struct *p, unsigned long clone_flags)
{
    // ...
    trace_sched_wakeup_new(rq, p);  // calling our above defined tracepoint here
    // ...
}
</code></pre>
<h3 id="tracepoints-in-modules">tracepoints in modules</h3>
<ul>
<li>when you run insmod or modprobe, the user-space utility eventually calls <code>sys_init_module</code> to pass the binary blob (the .ko file) to the kernel</li>
</ul>
<pre><code class="language-c">struct module
{
    // ...
#ifdef CONFIG_TRACEPOINTS
    struct tracepoint *tracepoints;
    unsigned int num_tracepoints;
#endif
    // ...
};
</code></pre>
<pre><code class="language-c">static noinline struct module *load_module(void __user *umod,
                  unsigned long len,
                  const char __user *uargs)
{
    // ...
#ifdef CONFIG_TRACEPOINTS
    mod-&gt;tracepoints = section_objs(hdr, sechdrs, secstrings,
                    &quot;__tracepoints&quot;,
                    sizeof(*mod-&gt;tracepoints),
                    &amp;mod-&gt;num_tracepoints);
#endif
    // ...
    if (!mod-&gt;taints) {
        // ...
#ifdef CONFIG_TRACEPOINTS
        tracepoint_update_probe_range(mod-&gt;tracepoints,
            mod-&gt;tracepoints + mod-&gt;num_tracepoints);
#endif
    }
    // ...
}

asmlinkage long
sys_init_module(void __user *umod,
        unsigned long len,
        const char __user *uargs)
{
    // ...
    mod = load_module(umod, len, uargs);
    // ...
}

oid module_update_tracepoints(void)
{
    // ...
    list_for_each_entry(mod, &amp;modules, list)
        if (!mod-&gt;taints)
            tracepoint_update_probe_range(mod-&gt;tracepoints,
                mod-&gt;tracepoints + mod-&gt;num_tracepoints);
    // ...
}
</code></pre>
<h3 id="debugfs-interface-for-tracepoints">debugfs interface for tracepoints</h3>
<ul>
<li>we can easily enable tracepoints by using the <code>event tracing</code> framework</li>
</ul>
<pre><code class="language-bash"># to enable event &#39;sched_wakeup&#39;
echo 1 &gt; /sys/kernel/debug/tracing/events/sched/sched_wakeup/enable

# to disable it
echo 0 &gt; /sys/kernel/debug/tracing/events/sched/sched_wakeup/enable

# to enable all events in sched subsystem
echo 1 &gt; /sys/kernel/debug/tracing/events/sched/enable

# to eanble all events
echo 1 &gt; /sys/kernel/debug/tracing/events/enable
</code></pre>
<h1 id="perf-subsystem">perf subsystem</h1>
<ul>
<li>NOTE: perf subsystem implementation based on Linux v2.6.32 codes reading, may update it after reading new version of it</li>
</ul>
<h2 id="brief-history-3">brief history</h2>
<ul>
<li>Linux v2.6.31 first introduced <code>Performance Counters for Linux(PCL)</code> into its mainline for just hardware counters, so the syscall is <code>sys_perf_counter_open</code></li>
<li>but Linux v2.6.32 changed the syscall to <code>sys_perf_event_open</code>, and expanded the framework to do more than just hw counters</li>
</ul>
<h2 id="how-does-perf-event-work">how does perf event work</h2>
<ul>
<li>the following calling procedure only means calling timeline, not caller-callee callgraph, so that we can see how perf event subsystem is inited: start_kernel –&gt; sched_init –&gt; <code>perf_event_init</code> –&gt; check_bugs –&gt; identify_boot_cpu –&gt; <code>init_hw_perf_events</code> –&gt; <code>intel_pmu_init</code>/<code>amd_pmu_init</code> –&gt; <code>perf_events_lapic_init</code> –&gt; rest_init –&gt; kernel_init –&gt; do_basic_setup –&gt; do_initcalls –&gt; <code>perf_event_sysfs_init</code></li>
</ul>
<h1 id="ebpf">eBPF</h1>
<ul>
<li>NOTE: eBPF implementation based on Linux v4.0 codes reading, may update it after reading new version of it</li>
</ul>
<h2 id="brief-history-4">brief history</h2>
<ul>
<li>v3.17 first introduced <code>kernel/bpf</code> directory into linux kernel, mainly for socket filter</li>
<li>v3.18 introduced <code>bpf</code> syscall, <code>bpf verifier</code> and basic <code>bpf map</code></li>
<li>v3.19 introduced <code>BPF_MAP_TYPE_ARRAY</code> bpf array and <code>BPF_MAP_TYPE_HASH</code> bpf hash map; basic version of <code>bpf helpers</code>; <code>BPF_PROG_TYPE_SOCKET_FILTER</code> socket filter macro</li>
<li>v4.1 introduced more prog types, especially <code>BPF_PROG_TYPE_KPROBE</code> allowing bpf program to attach to kprobes; more bpf helper functions like <code>bpf_get_prandom_u32</code> and <code>bpf_get_smp_processor_id</code> etc</li>
<li>v4.2 introduced <code>tail calls</code> to allow chaining multiple eBPF programs together, effectively extending the overall execution beyond the single-program instruction limit; new map <code>BPF_MAP_TYPE_PROG_ARRAY</code> type to support <code>tail calls</code>; several support helper functions, including <code>bpf_tail_call</code>, <code>bpf_get_current_pid_tgid</code>, <code>bpf_get_current_uid_gid</code>, <code>bpf_get_current_comm</code> etc</li>
<li>v4.3 new map <code>BPF_MAP_TYPE_PERF_EVENT_ARRAY</code> type and with <code>perf_event_read</code> function to do perf event monitoring; more helper functions like <code>bpf_get_cgroup_classid</code> <code>skb_[gs]et_tunnel_key</code> etc; first introduced <code>libbpf</code> in <code>tools/lib/bpf</code></li>
<li>v4.4 mount and register <code>/sys/fs/bpf/</code> filesystem for new cmd <code>BPF_OBJ_PIN</code> and <code>BPF_OBJ_GET</code>; <code>/proc/sys/kernel/unprivileged_bpf_disabled</code> to control whether users without <code>CAP_SYS_ADMIN</code> privilege can use the <code>bpf</code> syscall; <code> bpf_perf_event_output</code> <code> bpf_get_route_realm</code> and <code>bpf_redirect</code> functions</li>
<li>v4.5 enhance <code>/sys/fs/bpf/</code> filesystem to support <code>link</code> and <code>rename</code>; <code>bpf_skb_load_bytes</code> to <code>net/core/filter.c</code></li>
<li>v4.6 new map types <code>BPF_MAP_TYPE_PERCPU_ARRAY</code> <code>BPF_MAP_TYPE_PERCPU_HASH</code> <code>BPF_MAP_TYPE_STACK_TRACE</code>; helper functions like <code>bpf_skb_[gs]et_tunnel_opt</code>, <code>bpf_get_stackid</code> etc, after using map <code>BPF_MAP_TYPE_STACK_TRACE</code> and <code>bpf_get_stackid</code> to get stack traces, you can use <code>/proc/kallsyms</code> to translate them into understandable function names</li>
<li>v4.7 new program type <code>BPF_PROG_TYPE_TRACEPOINT</code>; sysctl interfaces <code>/proc/sys/net/core/bpf_jit_enable</code> and <code>/proc/sys/net/core/bpf_jit_harden</code></li>
</ul>
<h2 id="bpf-program-loading">bpf program loading</h2>
<ul>
<li>calling <code>bpf</code> system call with <code>BPF_PROG_LOAD</code> cmd arg, bpf program will be copied into kernel and run through eBPF verifier by function <code>bpf_check</code>. After passing verification, <code>bpf_prog_select_runtime</code> will be used to select interpreter running function <code>__bpf_prog_run</code> or <code>jit</code>ed BPF instructions to native codes</li>
</ul>
<pre><code class="language-c">static unsigned int __bpf_prog_run(void *ctx, const struct bpf_insn *insn)
{
    // ...
        /* CALL */
    JMP_CALL:
        /* Function call scratches BPF_R1-BPF_R5 registers,
         * preserves BPF_R6-BPF_R9, and stores return value
         * into BPF_R0.
         */
        BPF_R0 = (__bpf_call_base + insn-&gt;imm)(BPF_R1, BPF_R2, BPF_R3,
                               BPF_R4, BPF_R5);  // NOTE: where magics happend
        CONT;
    // ...
    // how does tail call implement, codes from v4.2
    JMP_TAIL_CALL: {
        struct bpf_map *map = (struct bpf_map *) (unsigned long) BPF_R2;
        struct bpf_array *array = container_of(map, struct bpf_array, map);
        struct bpf_prog *prog;
        u64 index = BPF_R3;
        // ...
        prog = READ_ONCE(array-&gt;prog[index]);  // get new prog here
        // ...
        ARG1 = BPF_R1;
        insn = prog-&gt;insnsi;  // replace insn to new prog&#39;s instructions
        goto select_insn;  // next run, we will execute insn of new program
out:
        CONT;
    }
    // ...
}
</code></pre>
<pre><code class="language-c">void bpf_int_jit_compile(struct bpf_prog *prog)
{
    // ...
    if (image) {
        // ... after jiting related codes
        prog-&gt;bpf_func = (void *)image;  // here we replace the interpreter func to jited codes
        prog-&gt;jited = true;
        // ...
    }
    // ...
}
</code></pre>
<pre><code class="language-c">void bpf_prog_select_runtime(struct bpf_prog *fp)
{
    fp-&gt;bpf_func = (void *) __bpf_prog_run;  // NOTE: save interpreter running function

    /* Probe if internal BPF can be JITed */
    bpf_int_jit_compile(fp);  // here fp-&gt;bpf_func is replaced with jited
                              // codes if jit is working
    // ...
}
</code></pre>
<h2 id="bpf-program-running">bpf program running</h2>
<ul>
<li>two macros <code>BPF_PROG_RUN</code> and <code>SK_RUN_FILTER</code> are used to run bpf programs</li>
</ul>
<pre><code class="language-c">#define BPF_PROG_RUN(filter, ctx)  (*filter-&gt;bpf_func)(ctx, filter-&gt;insnsi)

static int tcf_bpf(struct sk_buff *skb, const struct tc_action *a,
           struct tcf_result *res)
{
    struct tcf_bpf *b = a-&gt;priv;
    int action, filter_res;

    spin_lock(&amp;b-&gt;tcf_lock);
        
    // ...
    filter_res = BPF_PROG_RUN(b-&gt;filter, skb);
    // ...

    spin_unlock(&amp;b-&gt;tcf_lock);
    return action;
</code></pre>
<pre><code class="language-c">/* Macro to invoke filter function. */
#define SK_RUN_FILTER(filter, ctx) \
    (*filter-&gt;prog-&gt;bpf_func)(ctx, filter-&gt;prog-&gt;insnsi)

int sk_filter(struct sock *sk, struct sk_buff *skb)
{
    int err;
    struct sk_filter *filter;
        
    // ...

    rcu_read_lock();
    filter = rcu_dereference(sk-&gt;sk_filter);
    if (filter) {
        // as we can see here, how eBPF program works
        unsigned int pkt_len = SK_RUN_FILTER(filter, skb);

        err = pkt_len ? pskb_trim(skb, pkt_len) : -EPERM;
    }
    rcu_read_unlock();

    return err;
}
</code></pre>
<h2 id="bpf-program-with-kprobes">bpf program with kprobes</h2>
<h3 id="perequisite-knowledge">perequisite knowledge</h3>
<ul>
<li>when <code>start_kernel</code> executes, it will call <code>perf_event_init</code>, in which <code>perf_tp_register</code> will be called to register the corresponding pmu <code>perf_tracepoint</code></li>
</ul>
<pre><code class="language-c">static struct pmu perf_tracepoint = {
    .task_ctx_nr    = perf_sw_context,

    .event_init = perf_tp_event_init,  // tracepoint related event init func
    // ...
};

int perf_pmu_register(struct pmu *pmu, const char *name, int type)
{
    // ...
    pmu-&gt;name = name;
    // ...
    pmu-&gt;type = type;
    // ...
    list_add_rcu(&amp;pmu-&gt;entry, &amp;pmus);
    // ...
}

static inline void perf_tp_register(void)
{
    // register perf_tracepoint pmu to pmus
    perf_pmu_register(&amp;perf_tracepoint, &quot;tracepoint&quot;, PERF_TYPE_TRACEPOINT);
}

void __init perf_event_init(void)
{
    // ...
    perf_tp_register();
    // ...
}
</code></pre>
<ul>
<li><code>init_kprobe_trace</code> will be called after <code>perf_event_init</code> is done inside <code>rest_init</code>, it will register <code>probes_write</code> write handler, which will be triggered when we echo <code>/sys/kernel/debug/tracing/kprobe_events</code></li>
</ul>
<pre><code class="language-c">static const struct file_operations kprobe_events_ops = {
    // ...
    .write      = probes_write,
}

static __init int init_kprobe_trace(void)
{
    // ...
    entry = tracefs_create_file(&quot;kprobe_events&quot;, 0644, d_tracer,
                    NULL, &amp;kprobe_events_ops);
    // ...
}
fs_initcall(init_kprobe_trace);
</code></pre>
<h2 id="how-does-bpf-program-connect-to-kprobes">how does bpf program connect to kprobes</h2>
<ul>
<li>when you write SEC(“kprobe/xxxx”), you aren’t writing a standard C function call. when you compile your code using <code>clang -target bpf</code>, the compiler looks at that SEC() macro and creates a custom section named <code>kprobe/xxxx</code></li>
</ul>
<pre><code class="language-c">#define SEC(NAME) __attribute__((section(NAME), used))

SEC(&quot;kprobe/xxxx&quot;)
int bpf_prog(struct pt_regs *ctx)
{
    struct sk_buff *skb;
    skb = (struct sk_buff *) ctx-&gt;di;
    // ... get other related stuff this function need from ctx
}
</code></pre>
<ul>
<li>the loader (like the following <code>load_bpf_file</code>) loads the compiled .o file and scans all the section headers, when it sees the prefix <code>kprobe/</code>, it realizes this is a bpf kprobe program and load it</li>
</ul>
<pre><code class="language-c">int load_bpf_file(char *path)
{
    // ...
    fd = open(path, O_RDONLY, 0);
    // ...
    for (i = 1; i &lt; ehdr.e_shnum; i++) {
        if (memcmp(shname_prog, &quot;kprobe/&quot;, 7) == 0 ||
            memcmp(shname_prog, &quot;kretprobe/&quot;, 10) == 0 ||
            memcmp(shname_prog, &quot;socket&quot;, 6) == 0)
                load_and_attach(shname_prog, insns, data_prog-&gt;d_size);
    }
    // ...
}
</code></pre>
<ul>
<li>it parses the rest of the string xxxx as the target function, using <code>tracefs</code> to echo to <code>/sys/kernel/debug/tracing/kprobe_events</code> to first create the kprobe and it will triger the corresponding <code>.write</code> handler <code>probes_write</code> registered when kprobe_events file created, through this, it will finally register the kprobe</li>
</ul>
<pre><code class="language-c">static struct trace_kprobe *alloc_trace_kprobe(const char *group,
                         const char *event, void *addr,
                         const char *symbol, unsigned long offs,
                         int nargs, bool is_return)
{
    // ...
    if (is_return)
        tk-&gt;rp.handler = kretprobe_dispatcher;
    else
        tk-&gt;rp.kp.pre_handler = kprobe_dispatcher;  // when the kprobe hit, it will call this kprobe_dispatcher
    // ...
}

static int register_kprobe_event(struct trace_kprobe *tk)
{
    struct ftrace_event_call *call = &amp;tk-&gt;tp.call;
    // ...
    call-&gt;flags = TRACE_EVENT_FL_KPROBE;
    call-&gt;class-&gt;reg = kprobe_register;
    call-&gt;data = tk;
    // ...
}

static int __register_trace_kprobe(struct trace_kprobe *tk)
{
    // ...
    if (trace_kprobe_is_return(tk))
        ret = register_kretprobe(&amp;tk-&gt;rp);
    else
        ret = register_kprobe(&amp;tk-&gt;rp.kp);  // finally register the kprobe here
    // ...
}

static int register_trace_kprobe(struct trace_kprobe *tk)
{
    // ...
    ret = register_kprobe_event(tk);
    // ...
    ret = __register_trace_kprobe(tk);
    // ...
}

static int create_trace_kprobe(int argc, char **argv)
{
    // ... parse arguments echo to kprobe_events file etc
        
    tk = alloc_trace_kprobe(group, event, addr, symbol, offset, argc,
                   is_return);
    // ...
    ret = register_trace_kprobe(tk);  // here to register the kprobe
    // ...
}

static ssize_t probes_write(struct file *file, const char __user *buffer,
                size_t count, loff_t *ppos)
{
    return traceprobe_probes_write(file, buffer, count, ppos,
            create_trace_kprobe);  // finally calls the create_trace_kprobe
}
</code></pre>
<ul>
<li>after this step, the kprobe has been registered, but the bpf program hasn’t been attached yet, <code>bpf_prog_load</code> first load and verify it</li>
</ul>
<pre><code class="language-c">static int load_and_attach(const char *event, struct bpf_insn *prog, int size)
{
    bool is_socket = strncmp(event, &quot;socket&quot;, 6) == 0;
    bool is_kprobe = strncmp(event, &quot;kprobe/&quot;, 7) == 0;
    bool is_kretprobe = strncmp(event, &quot;kretprobe/&quot;, 10) == 0;
    enum bpf_prog_type prog_type;
    // ...
        
    struct perf_event_attr attr = {};

    attr.type = PERF_TYPE_TRACEPOINT;
    attr.sample_type = PERF_SAMPLE_RAW;
    attr.sample_period = 1;
    attr.wakeup_events = 1;

    if (is_socket) {
        prog_type = BPF_PROG_TYPE_SOCKET_FILTER;
    } else if (is_kprobe || is_kretprobe) {
        prog_type = BPF_PROG_TYPE_KPROBE;  // kprobe bpf type program
    } // ...
        
    if (is_kprobe || is_kretprobe) {
        if (is_kprobe)
            event += 7;
        else
            event += 10;

        snprintf(buf, sizeof(buf),
             &quot;echo &#39;%c:%s %s&#39; &gt;&gt; /sys/kernel/debug/tracing/kprobe_events&quot;,
             is_kprobe ? &#39;p&#39; : &#39;r&#39;, event, event);  // here echo to kprobe_events file
        err = system(buf);
        // ...
                
        fd = bpf_prog_load(prog_type, prog, size, license, kern_version);
    }
    // ...
    strcpy(buf, DEBUGFS);
    strcat(buf, &quot;events/kprobes/&quot;);
    strcat(buf, event);
    strcat(buf, &quot;/id&quot;);

    efd = open(buf, O_RDONLY, 0);
    // ...
    err = read(efd, buf, sizeof(buf));
    // ...
    buf[err] = 0;
    id = atoi(buf);
    attr.config = id;  // continue to config struct perf_event_attr
        
    efd = perf_event_open(&amp;attr, -1/*pid*/, 0/*cpu*/, -1/*group_fd*/, 0);
    // ...
    ioctl(efd, PERF_EVENT_IOC_ENABLE, 0);
    ioctl(efd, PERF_EVENT_IOC_SET_BPF, fd);

    return 0;
}
</code></pre>
<ul>
<li>calling <code>perf_event_open</code> syscall to create and sets up the <code>perf_event</code> structure, the event is usually created in a <code>DISABLED</code> state, it currently only knows how to send data to the standard perf ring buffer, but not a BPF program</li>
</ul>
<pre><code class="language-c">static int
enable_trace_kprobe(struct trace_kprobe *tk, struct ftrace_event_file *file)
{
    // ...
        tk-&gt;tp.flags |= TP_FLAG_PROFILE;

    if (trace_probe_is_registered(&amp;tk-&gt;tp) &amp;&amp; !trace_kprobe_has_gone(tk)) {
        if (trace_kprobe_is_return(tk))
            ret = enable_kretprobe(&amp;tk-&gt;rp);
        else
            ret = enable_kprobe(&amp;tk-&gt;rp.kp);  // here to finally enable the kprobe
    }
    // ...
}

static int kprobe_register(struct ftrace_event_call *event,
               enum trace_reg type, void *data)
{
    struct trace_kprobe *tk = (struct trace_kprobe *)event-&gt;data;
    struct ftrace_event_file *file = data;

    switch (type) {
    // ...
#ifdef CONFIG_PERF_EVENTS
    case TRACE_REG_PERF_REGISTER:
        return enable_trace_kprobe(tk, NULL);
    // ...
#endif
    }
    // ...
}

static int perf_trace_event_reg(struct ftrace_event_call *tp_event,
                struct perf_event *p_event)
{
    // ...
    // kprobe_register assigned in register_kprobe_event
    ret = tp_event-&gt;class-&gt;reg(tp_event, TRACE_REG_PERF_REGISTER, NULL);
    // ...
}

static int perf_trace_event_init(struct ftrace_event_call *tp_event,
                 struct perf_event *p_event)
{
    // ...
    ret = perf_trace_event_reg(tp_event, p_event);
    // ...
}

int perf_trace_init(struct perf_event *p_event)
{
    struct ftrace_event_call *tp_event;
    u64 event_id = p_event-&gt;attr.config;
    // ...
    list_for_each_entry(tp_event, &amp;ftrace_events, list) {
        if (tp_event-&gt;event.type == event_id &amp;&amp;
            tp_event-&gt;class &amp;&amp; tp_event-&gt;class-&gt;reg &amp;&amp;
            try_module_get(tp_event-&gt;mod)) {
            ret = perf_trace_event_init(tp_event, p_event);
            // ...
        }
    }
    // ...
}

static int perf_tp_event_init(struct perf_event *event)
{
    int err;

    if (event-&gt;attr.type != PERF_TYPE_TRACEPOINT)
        return -ENOENT;
    // ...
    err = perf_trace_init(event);
    // ...
    event-&gt;destroy = tp_perf_event_destroy;

    return 0;
}

static int perf_try_init_event(struct pmu *pmu, struct perf_event *event)
{
    // ...
    event-&gt;pmu = pmu;
    // here finally perf_tp_event_init in struct pmu perf_tracepoint is called
    ret = pmu-&gt;event_init(event);
    // ...
}

struct pmu *perf_init_event(struct perf_event *event)
{
    // ...
    list_for_each_entry_rcu(pmu, &amp;pmus, entry) {
        ret = perf_try_init_event(pmu, event);
        // ...
    }
    // ...
}

static inline void perf_event__state_init(struct perf_event *event)
{
    event-&gt;state = event-&gt;attr.disabled ? PERF_EVENT_STATE_OFF :
                          PERF_EVENT_STATE_INACTIVE;
}

static struct perf_event *
perf_event_alloc(struct perf_event_attr *attr, int cpu,
         struct task_struct *task,
         struct perf_event *group_leader,
         struct perf_event *parent_event,
         perf_overflow_handler_t overflow_handler,
         void *context, int cgroup_fd)
{
    // ...
    perf_event__state_init(event);  // the event is not enabled yet
    // ...
    pmu = perf_init_event(event);
    // ...
}

SYSCALL_DEFINE5(perf_event_open,
        struct perf_event_attr __user *, attr_uptr,
        pid_t, pid, int, cpu, int, group_fd, unsigned long, flags)
{
    // ...
    event = perf_event_alloc(&amp;attr, cpu, task, group_leader, NULL,
                 NULL, NULL, cgroup_fd);
    // ...
    event_file = anon_inode_getfile(&quot;[perf_event]&quot;, &amp;perf_fops, event,
                    f_flags);  // here create the event_file and register perf_fops for it
    // ...
}
</code></pre>
<ul>
<li>in <code>perf_event_open</code> syscall, it will create an event_file by calling <code>anon_inode_getfile</code> and register <code>perf_fops</code> for it, which is important to attach bpf to kprobe and finally enable it</li>
</ul>
<pre><code class="language-c">static long perf_ioctl(struct file *file, unsigned int cmd, unsigned long arg)
{
    struct perf_event *event = file-&gt;private_data;
    // ...
    ret = _perf_ioctl(event, cmd, arg);
    // ...
}

static const struct file_operations perf_fops = {
    // ...
    .unlocked_ioctl     = perf_ioctl,
    // ...
};
</code></pre>
<ul>
<li>using <code>PERF_EVENT_IOC_ENABLE</code> to activate the perf event, and using <code>PERF_EVENT_IOC_SET_BPF</code> to attach bpf program to kprobe</li>
</ul>
<pre><code class="language-c">// ... copied from above
    ioctl(efd, PERF_EVENT_IOC_ENABLE, 0);
    ioctl(efd, PERF_EVENT_IOC_SET_BPF, fd);
// ...

static int perf_event_set_bpf_prog(struct perf_event *event, u32 prog_fd)
{
    struct bpf_prog *prog;

    // ...
    prog = bpf_prog_get(prog_fd);  // loads the ebpf program
    // ...
    event-&gt;tp_event-&gt;prog = prog;  // here finally attaches the ebpf program

    return 0;
}

static long _perf_ioctl(struct perf_event *event, unsigned int cmd, unsigned long arg)
{
    void (*func)(struct perf_event *);
    u32 flags = arg;

    switch (cmd) {
    case PERF_EVENT_IOC_ENABLE:
        func = _perf_event_enable;  // later calls func, event-&gt;state = PERF_EVENT_STATE_ACTIVE;
        break;
    // ...
    case PERF_EVENT_IOC_SET_BPF:
        return perf_event_set_bpf_prog(event, arg);
    // ...
    }
}
</code></pre>
<ul>
<li>when the kprobe is hit, <code>kprobe_dispatcher</code> will be called, and finally to call the attached bpf program</li>
</ul>
<pre><code class="language-c">unsigned int trace_call_bpf(struct bpf_prog *prog, void *ctx)
{
    unsigned int ret;
    // ...
    ret = BPF_PROG_RUN(prog, ctx);  // here to finally run bpf program
    // ...
}

static void
kprobe_perf_func(struct trace_kprobe *tk, struct pt_regs *regs)
{
    struct ftrace_event_call *call = &amp;tk-&gt;tp.call;
    struct bpf_prog *prog = call-&gt;prog;
    // ...

    if (prog &amp;&amp; !trace_call_bpf(prog, regs))  // call bpf program
        return;
    // ...
}

static int kprobe_dispatcher(struct kprobe *kp, struct pt_regs *regs)
{
    struct trace_kprobe *tk = container_of(kp, struct trace_kprobe, rp.kp);

    tk-&gt;nhit++;
    // ...
#ifdef CONFIG_PERF_EVENTS
    if (tk-&gt;tp.flags &amp; TP_FLAG_PROFILE)
        kprobe_perf_func(tk, regs);  // inside to call bpf program
#endif
    return 0;   /* We don&#39;t tweek kernel, so just return 0 */
}
</code></pre>
<h2 id="how-does-bpf-program-connect-to-tracepoint">how does bpf program connect to tracepoint</h2>
<ul>
<li>ebpf program attach to tracepoints is very similar to kprobes in Linux <code>v4.7</code>, here we does not contain all the details like kprobes above, but something specially to tracepoints</li>
<li>assigning <code>trace_event_reg</code> to <code>.reg</code> field of <code>struct trace_event_class</code>, and it will call <code>tracepoint_probe_register</code> to register field <code>.perf_probe</code> callback function, which is a <code>perf_trace_xxx</code> function, to tracepoint</li>
</ul>
<pre><code class="language-c">#define _TRACE_PERF_INIT(call)                      \
    .perf_probe     = perf_trace_##call,

static struct trace_event_class __used __refdata event_class_##call = { \
    .system         = TRACE_SYSTEM_STRING,          \   
    .define_fields      = trace_event_define_fields_##call, \
    .fields         = LIST_HEAD_INIT(event_class_##call.fields),\
    .raw_init       = trace_event_raw_init,         \   
    .probe          = trace_event_raw_event_##call,     \   
    .reg            = trace_event_reg,          \
    _TRACE_PERF_INIT(call)                      \   
};

int trace_event_reg(struct trace_event_call *call,
            enum trace_reg type, void *data)
{
    struct trace_event_file *file = data;

    WARN_ON(!(call-&gt;flags &amp; TRACE_EVENT_FL_TRACEPOINT));
    switch (type) {
    // ...
#ifdef CONFIG_PERF_EVENTS
    case TRACE_REG_PERF_REGISTER:
        return tracepoint_probe_register(call-&gt;tp,
                         call-&gt;class-&gt;perf_probe,  // to register perf_probe callback to tracepoint
                         call);
    case TRACE_REG_PERF_UNREGISTER:
        tracepoint_probe_unregister(call-&gt;tp,
                        call-&gt;class-&gt;perf_probe,
                        call);
        return 0;
    // ...
#endif
    }    
    return 0;
}
EXPORT_SYMBOL_GPL(trace_event_reg);
</code></pre>
<ul>
<li>when the tracepoint is hit, the registered <code>.perf_probe</code> cb will be called, as stated above, which is <code>perf_trace_xxx</code> function</li>
</ul>
<pre><code class="language-c">static notrace void                         \
perf_trace_##call(void *__data, proto)                  \
{                                   \
    struct trace_event_call *event_call = __data;           \
    // ...
    struct bpf_prog *prog = event_call-&gt;prog;           \
    struct pt_regs *__regs;                     \
    // ...
    perf_fetch_caller_regs(__regs);                 \
    // ...
    perf_trace_run_bpf_submit(entry, __entry_size, rctx,        \
                  event_call, __count, __regs,      \
                  head, __task);            \
}

void perf_trace_run_bpf_submit(void *raw_data, int size, int rctx, 
                   struct trace_event_call *call, u64 count,
                   struct pt_regs *regs, struct hlist_head *head,
                   struct task_struct *task)
{
    struct bpf_prog *prog = call-&gt;prog;

    if (prog) {
        *(struct pt_regs **)raw_data = regs;
        // here to finally call trace_call_bpf to run ebpf program
        if (!trace_call_bpf(prog, raw_data) || hlist_empty(head)) {
            // ...
        }     
    }
    // ...
}
</code></pre>
<h2 id="ebpf-sysctl-interface">ebpf sysctl interface</h2>
<h3 id="bpf_jit_enable">bpf_jit_enable</h3>
<ul>
<li>check ebpf jit status</li>
</ul>
<pre><code class="language-bash">cat /proc/sys/net/core/bpf_jit_enable
</code></pre>
<ul>
<li>turn on or off ebpf jit compiler</li>
</ul>
<pre><code class="language-bash"># turn off
echo 0 &gt; /proc/sys/net/core/bpf_jit_enable  # for interpreter only
# turn on
echo 1 &gt; /proc/sys/net/core/bpf_jit_enable  # enable jit
# debug mode
# the kernel will output the resulting native opcodes to the kernel log (dmesg)
echo 2 &gt; /proc/sys/net/core/bpf_jit_enable
</code></pre>
<h3 id="bpf_jit_harden">bpf_jit_harden</h3>
<ul>
<li>check ebpf jit harden status</li>
</ul>
<pre><code class="language-bash">cat /proc/sys/net/core/bpf_jit_harden
</code></pre>
<ul>
<li>turn on or off ebpf jit hardening</li>
</ul>
<pre><code class="language-bash">echo 0 &gt; /proc/sys/net/core/bpf_jit_harden  # disable JIT hardening
# enable JIT hardening for unprivileged users only
echo 1 &gt; /proc/sys/net/core/bpf_jit_harden
# enable JIT hardening for all users
echo 2 &gt; /proc/sys/net/core/bpf_jit_harden
</code></pre>
<h1 id="ftrace">ftrace</h1>
<ul>
<li>NOTE: ftrace implementation based on Linux v2.6.27 codes reading, may update it after reading new version of it</li>
</ul>
<h2 id="perequisite-knowledge-1">perequisite knowledge</h2>
<ul>
<li>Linux kernel should be compiled with gcc with <code>-pg</code> flag, which will insert <code>mcount</code> function call at the start of every function, unless you use the function attribute <code>no_instrument_function</code> to suppress profiling of individual functions when compiling</li>
</ul>
<pre><code class="language-shell">gcc -c -pg -m32 ftrace.c
</code></pre>
<pre><code class="language-c">void foo()
{
    printf(&quot;hello world\n&quot;);
}

#define notrace __attribute__((no_instrument_function))

void notrace bar()
{
    printf(&quot;hello&quot;);
}
</code></pre>
<pre><code class="language-asm">.LC0:
        .string &quot;hello world&quot;
foo:
        pushl   %ebp
        movl    %esp, %ebp
        subl    $8, %esp
1:      call        mcount  // NOTE: here -pg flag will insert mcount function call
        subl    $12, %esp
        pushl   $.LC0
        // ...
bar:
        pushl   %ebp
        movl    %esp, %ebp
        subl    $8, %esp
        subl    $12, %esp
        pushl   $.LC0
        // ...
</code></pre>

            ]]>
        </content>
    </entry>
    <entry>
        <title>git recipes</title>
        <author>
            <name>willmafh</name>
        </author>
        <link href="https://willmafh.github.io/git-recipes-2.html"/>
        <id>https://willmafh.github.io/git-recipes-2.html</id>
            <category term="git"/>

        <updated>2026-01-05T16:23:02+08:00</updated>
            <summary type="html">
                <![CDATA[
                    to fully sync commit logs from an upstream repo to your forked repo # add upstream remote if not ready git remote add upstream https://github.com/ORIGINAL_OWNER/ORIGINAL_REPO.git # verify remote, there are origin and remote git remote -v # fetch all updates from both remotes git fetch&hellip;
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <ul>
<li>to fully sync commit logs from an upstream repo to your forked repo</li>
</ul>
<pre><code class="language-shell"># add upstream remote if not ready
git remote add upstream https://github.com/ORIGINAL_OWNER/ORIGINAL_REPO.git

# verify remote, there are origin and remote
git remote -v

# fetch all updates from both remotes
git fetch --all

git checkout main    # switch to main branch

# reset to exactly match upstream
git reset --hard upstream/main

# force push to your fork
git push --force origin main

# clean up local branch references
git fetch --prune origin
git fetch --prune upstream
</code></pre>

            ]]>
        </content>
    </entry>
    <entry>
        <title>Effective Lua</title>
        <author>
            <name>willmafh</name>
        </author>
        <link href="https://willmafh.github.io/lua-tricks.html"/>
        <id>https://willmafh.github.io/lua-tricks.html</id>
            <category term="Lua"/>

        <updated>2025-11-23T20:36:53+08:00</updated>
            <summary type="html">
                <![CDATA[
                    NOTE: This is not a Lua tutorial or reference, just a place to record something interesting when I read Lua source code, which is version 5.4.3 -- both of cases are correct lua -e&#39;print(&quot;hello world&quot;) lua -e &#39;print(&quot;hello world&quot;)&#39; command prompt command prompt default values&hellip;
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p><em>NOTE: This is not a Lua tutorial or reference, just a place to record something interesting when I read Lua source code, which is version 5.4.3</em></p><h2 id="lua-command">lua command</h2>
<h3 id="-e-flag">-e flag</h3>
<pre><code class="language-shell">-- both of cases are correct
lua -e&#39;print(&quot;hello world&quot;)

lua -e &#39;print(&quot;hello world&quot;)&#39;
</code></pre>
<h3 id="command-prompt">command prompt</h3>
<ul>
<li>command prompt default values<ul>
<li><code>_PROMPT</code> is “&gt; “</li>
<li><code>_PROMPT2</code> is “&gt;&gt; “</li>
</ul>
</li>
<li>we can set global values like below to change lua command prompts</li>
</ul>
<pre><code class="language-shell">export LUA_INIT=&#39;_G._PROMPT = &quot;~ &quot;; _G._PROMPT2 = &quot;~~ &quot;&#39;

lua  -- then prompts will be changed to new values
</code></pre>
<h2 id="lua-global-stuff">Lua global stuff</h2>
<h3 id="print-function">print function</h3>
<ul>
<li>print will first call <code>__tostring</code> metamethod in value’s metatable if it exists.</li>
</ul>
<pre><code class="language-lua">local t = {}
local mt = {
    __tostring = function()
        return &quot;willmafh&#39;s table&quot;
    end
}
setmetatable(t, mt)

print(t)  -- will print `willmafh&#39;s table`
</code></pre>
<ul>
<li>number, string, boolean, and nil will print their values as normal.</li>
<li>for other type values, it will try to get <code>__name</code> first, if it’s not a string, then Lua default type name will be used.</li>
</ul>
<pre><code class="language-lua">local t = {}
local mt = {
    __name = &quot;willmafh&#39;s table&quot;
}
setmetatable(t, mt)

print(t)  -- `willmafh&#39;s table: 0x5f3167d3acf0`
</code></pre>
<pre><code class="language-lua">local t = {}
local mt = {
    -- here __name should be a string, otherwise it&#39;s meaningless
    __name = function() return &quot;willmafh&#39;s table&quot; end
}
setmetatable(t, mt)

print(t)  -- `table: 0x5f3167d3acf0`
</code></pre>
<h3 id="lua-global-table">Lua global table</h3>
<pre><code class="language-lua">-- _G is Lua&#39;s global table
print(_G)

-- following can recursively get the global table
print(_G[&quot;_G&quot;])
print(_G._G)
</code></pre>
<h3 id="lua-version">Lua version</h3>
<pre><code>print(_G._VERSION)
</code></pre>
<h3 id="warn-function">warn function</h3>
<ul>
<li>should use control message <code>&quot;@on&quot;</code> to turn on warning message emitting first</li>
<li><code>warn(msg1, msg2, ...)</code> function can concat multiple string message</li>
<li>then should use control message <code>&quot;@off&quot;</code> to turn off warn function</li>
<li>there are only two control messages, <code>&quot;@on&quot;</code>/<code>&quot;@off&quot;</code>, other messages start with ‘@’ are normal messages</li>
</ul>
<pre><code class="language-lua">warn(&quot;@on&quot;)
warn(&quot;hello world&quot;)  -- Lua warning: hello world
warn(&quot;hello&quot;, &quot; from willmafh&quot;)  -- Lua warning: hello from willmafh
warn(&quot;@off&quot;)
warn(&quot;hello world&quot;)  -- won&#39;t log this warning message
</code></pre>
<h3 id="setmetatable-function">setmetatable function</h3>
<ul>
<li><code>setmetatable</code> will return its first argument, that is the table itself, it won’t create a new table</li>
</ul>
<pre><code class="language-lua">local t = {}
local mt = {}
local rt = setmetatable(t, mt)
print(t, rt)  -- here t and rt are the same value
</code></pre>
<ul>
<li>if the object’s metatable has <code>__metatable</code> field, it means the metatable is protected, and we can’t use setmetatable again to change it</li>
</ul>
<pre><code class="language-lua">local mt = {
    __metatable = &quot;the metatable is protected&quot;,
    __tostring = function()
        return &quot;willmafh&#39;s table&quot;
    end
}
local t = setmetatable({}, mt)
setmetatable(t, {})  -- error throw here
</code></pre>
<ul>
<li><code>nil</code> metatable argument will clear table’s original metatable if there is any</li>
</ul>
<pre><code class="language-lua">local mt = {
    __tostring = function()
        return &quot;willmafh&#39;s table from __tostring&quot;
    end
}
local t = setmetatable({}, mt)
print(getmetatable(t))  -- table: 0x603284328ef0

setmetatable(t, nil)
print(getmetatable(t))  -- nil
</code></pre>
<h3 id="getmetatable-function">getmetatable function</h3>
<ul>
<li>if the object’s metatable has <code>__metatable</code>, then returns its value, it
can be any type. otherwise returns the metatable itself</li>
</ul>
<pre><code class="language-lua">local mt = {
    __metatable = &quot;the metatable is protected&quot;,  -- can be any type value
    __tostring = function()
        return &quot;willmafh&#39;s table&quot;
    end
}
local t = setmetatable({}, mt)
print(getmetatable(t))  -- __metatable value is printed
</code></pre>
<h3 id="require-function">require function</h3>
<ul>
<li><code>require</code> function will first search <code>registry[&quot;_LOADED&quot;]</code> table, and if there is a required module, then it return directly</li>
<li><code>package</code> table is the upvalue of <code>require</code> function</li>
</ul>
<pre><code class="language-lua">local registry = debug.getregistry()

--[[
string  table
table: 0x5efa18719710
table: 0x5efa18719710
--]]
local name, upvalue = debug.getupvalue(require, 1)
print(type(name), type(upvalue))
print(upvalue)
print(_G[&quot;package&quot;])
</code></pre>
<h3 id="error-function">error function</h3>
<ul>
<li>not only can log string messages, but can log error objects, which has <code>__tostring</code> metamethod in its metatable</li>
</ul>
<pre><code class="language-lua">local errobj = setmetatable({}, {
    __tostring = function()
        return &quot;error from tostring meta method&quot;
    end
})

error(errobj)
</code></pre>
<ul>
<li>why does <code>error</code> never returns? Actually, all things the function under error does is to prepare an error message and put it on the top of the stack, then calling <code>LUAI_THROW</code> macro, which is implemented by <code>longjmp</code> in c, so we will back to where <code>LUAI_TRY</code> is called, which is obviously implemented by <code>setjmp</code> in c, finally will call <code>report</code> function to log the error message</li>
</ul>
<h3 id="arg-table">arg table</h3>
<ul>
<li>lua will collect all arguments in <code>arg</code> global table. The script name will be at index 0, and the first argument will be at index 1 and so on. The interpreter name and its options before the script will go to negative indices. <code>Indices &lt;= 0</code> belong to the associative part of the table, and <code>indices &gt; 0</code> belong to the array part</li>
</ul>
<pre><code class="language-shell">lua -W arg_test.lua hello world
</code></pre>
<pre><code class="language-lua">local arg = _G.arg

print(#arg)  -- 2, array part of the arg table
--[[
1       hello
2       world
--]]
for i, v in ipairs(arg) do
    print(i, v)
end

--[[
1       hello
2       world
0       arg_test.lua
-2      lua
-1      -W
--]]
for k, v in pairs(arg) do
    print(k, v)
end
</code></pre>
<h2 id="registry-table">registry table</h2>
<ul>
<li>the <code>first</code> element in registry table array is the main Lua thread, and the <code>second</code> element in registry table array is the global table <code>_G</code></li>
</ul>
<pre><code class="language-lua">local registry = debug.getregistry()

-- thread: 0x5886909e72a8
print(registry[1])  -- main thread

--[[
table: 0x5886909e7c50
table: 0x5886909e7c50
--]]
print(registry[2]) -- global table _G
print(_G)
</code></pre>
<ul>
<li>all <code>require</code> loaded modules, including lua file module and c libs module, are kept in <code>registry[&quot;_LOADED&quot;]</code> table.</li>
</ul>
<pre><code class="language-lua">require &quot;lfs&quot;
require &quot;cjson.safe&quot;

local registry = debug.getregistry()

for k, v in pairs(registry[&quot;_LOADED&quot;]) do
    print(k, v)
end

--[[
_G      table: 0x6461ef84bc50
os      table: 0x5786711edbb0
...
lfs     table: 0x6461ef8526f0
cjson.safe      table: 0x6461ef855a80
...
--]]
</code></pre>
<ul>
<li>all loaded c libs will be kept in <code>registry[&quot;_CLIBS&quot;]</code> table and each lib will be kept in two ways, one is key value way, the other is an array element</li>
</ul>
<pre><code class="language-lua">local lfs = require &quot;lfs&quot;
local cjson = require &quot;cjson.safe&quot;

local registry = debug.getregistry()

local clibs = registry[&quot;_CLIBS&quot;]

--[[
1       userdata: 0x567153cf7fe0
2       userdata: 0x567153cfa700
/usr/local/lib/lua/5.4/lfs.so   userdata: 0x567153cf7fe0
/usr/local/lib/lua/5.4/cjson.so userdata: 0x567153cfa700
--]]
for k, v in pairs(clibs) do
    print(k, v)
end

--[[
1       userdata: 0x567153cf7fe0
2       userdata: 0x567153cfa700
--]]
for i, v in ipairs(clibs) do
    print(i, v)
end
</code></pre>
<ul>
<li><code>registry[&quot;_PRELOAD&quot;]</code> is normally an empty table, but it can be used to store loaders for lua modules, and when <code>require</code> function load a new module, it will first search this table, so you can use it to do something interesting</li>
</ul>
<pre><code class="language-lua">package.preload[&quot;devtools&quot;] = function()
    return { editor = &quot;vim&quot;, os = &quot;linux&quot; }
end

local devtools = require &quot;devtools&quot;
print(devtools.editor, devtools.os)
</code></pre>
<h2 id="environment-variables">environment variables</h2>
<h3 id="lua_noenv">LUA_NOENV</h3>
<ul>
<li>if <code>lua</code> is used with <code>-E</code> option, then <code>LUA_NOENV</code> will be set true in registry</li>
</ul>
<pre><code class="language-lua">#!/usr/bin/lua5.4 -E

local registry = debug.getregistry()
print(registry[&quot;LUA_NOENV&quot;])  -- true, since -E is used
</code></pre>
<h3 id="lua_path_5_4lua_path-lua_cpath_5_4lua_cpath">LUA_PATH_5_4/LUA_PATH, LUA_CPATH_5_4/LUA_CPATH</h3>
<ul>
<li><code>;;</code> in envs <code>LUA_PATH_5_4</code>/<code>LUA_PATH</code> means to insert default value from macro <code>LUA_PATH_DEFAULT</code> to <code>package.path</code>‘s final value</li>
<li><code>;;</code> in envs <code>LUA_CPATH_5_4</code>/<code>LUA_CPATH</code> means to insert default value from macro <code>LUA_CPATH_DEFAULT</code> to <code>package.cpath</code>‘s final   value</li>
<li>when lua is initialized, function luaopen_package will handle all these details</li>
</ul>
<pre><code class="language-lua">--[[ /usr/local/share/lua/5.4/?.lua;/usr/local/share/lua/5.4/?/init.lua;/usr/local/lib/lua/5.4/?.lua;/usr/local/lib/lua/5.4/?/init.lua;./?.lua;./?/init.lua
--]]
print(package.path)

-- /usr/local/lib/lua/5.4/?.so;/usr/local/lib/lua/5.4/loadall.so;./?.so
print(package.cpath)
</code></pre>
<h3 id="lua_init_5_4lua_init">LUA_INIT_5_4/LUA_INIT</h3>
<ul>
<li>setup init env to do something before anything else</li>
</ul>
<pre><code class="language-shell">export LUA_INIT_5_4=&#39;@init.lua&#39;   -- using `@` to load an init file

export LUA_INIT=&#39;print(&quot;hello world from init&quot;)&#39;  -- init lua string
</code></pre>
<h2 id="package-table">package table</h2>
<h3 id="packageconfig">package.config</h3>
<ul>
<li>a string describing some compile-time configurations for packages, please refer to lua manual for details</li>
</ul>
<pre><code class="language-lua">print(package.config)
</code></pre>
<h3 id="packageloaded">package.loaded</h3>
<ul>
<li><code>package.loaded</code> is a reference to <code>registry[&quot;_LOADED&quot;]</code></li>
</ul>
<pre><code class="language-lua">local registry = debug.getregistry()

--[[
table: 0x5c638d059a60
table: 0x5c638d059a60
--]]
print(registry[&quot;_LOADED&quot;])
print(package.loaded)
</code></pre>
<h3 id="packagepreload">package.preload</h3>
<ul>
<li><code>package.preload</code> is a reference to <code>registry[&quot;_PRELOAD&quot;]</code></li>
</ul>
<pre><code class="language-lua">local registry = debug.getregistry()

--[[
table: 0x64a359eb7be0
table: 0x64a359eb7be0
--]]
print(registry[&quot;_PRELOAD&quot;])
print(package.preload)
</code></pre>
<h3 id="packagesearchers">package.searchers</h3>
<ul>
<li>a table contains four searching functions (currently), which is used by <code>require</code> method to search modules, and they are searched in the following order<ul>
<li>preload table</li>
<li>lua module, searching <code>package.path</code> one by one</li>
<li>c module, searching <code>package.cpath</code> one by one</li>
<li>c root module, still searching <code>package.cpath</code>, but build name by stripping components after the first dot ‘.’</li>
</ul>
</li>
<li>using <code>require &quot;cjson.safe&quot;</code> as an example to illustrate the differences between searching c and searching c root</li>
</ul>
<pre><code class="language-lua">-- searching c: &#39;/usr/local/lib/lua/5.4/`cjson/safe.so`&#39;, will try to find safe.so under cjson directory
-- searching c root: &#39;/usr/local/lib/lua/5.4/`cjson.so`&#39;, will try to find cjson.so
local cjson = require &quot;cjson.safe&quot;
</code></pre>
<ul>
<li>both of searching c and c  root will finally look for function beginning with prefix <code>luaopen_</code>, such as ‘luaopen_cjson’ or ‘luaopen_cjson_safe’</li>
</ul>
<h3 id="packagesearchpath">package.searchpath</h3>
<ul>
<li>searching name in the given path, which is a template like <code>package.path</code><ul>
<li>dot ‘.’ in name will be replaced by ‘/‘</li>
<li>‘?’ in path will be replaced by dot replaced name</li>
</ul>
</li>
</ul>
<pre><code class="language-lua">local name = &quot;foo.a&quot;
local path = &quot;./?.lua;./?.lc;/usr/local/?/init.lua&quot;

-- so the search order will be: &#39;./foo/a.lua&#39;, &#39;./foo/a.lc&#39;, &#39;/usr/local/foo/a/init.lua&#39;
package.searchpath(name, path)
</code></pre>
<h3 id="packageloadlib">package.loadlib</h3>
<ul>
<li>it <code>does not</code> perform any path searching and <code>does not</code> automatically adds extensions like <code>require</code>, so libname arg must be the complete path</li>
<li>if funcname is “*” , then only loads and exports all symbols in the lib. otherwise looks for funcname and return it as a lua c function</li>
<li>libs loaded by this function will be added to <code>registry[&quot;_CLIBS&quot;]</code> table</li>
</ul>
<pre><code class="language-lua">local cjson_path = &quot;/usr/local/lib/lua/5.4/cjson.so&quot;

print(package.loadlib(cjson_path, &quot;*&quot;))  -- true

-- error here, since &quot;json_encode&quot; is not exported by cjson.so, but hey this is just an example
local json_encode = package.loadlib(cjson_path, &quot;json_encode&quot;)
-- using function following...
</code></pre>
<h2 id="table-module">table module</h2>
<h3 id="tableconcat">table.concat</h3>
<ul>
<li><code>sep</code> can be one char or more chars</li>
</ul>
<pre><code class="language-lua">local t = {&quot;hello&quot;, &quot;world&quot;}
local r = table.concat(t, &quot;,&quot;)

print(r)  -- &quot;hello,world&quot;

local t = {&quot;hello&quot;, &quot;world&quot;}
local r = table.concat(t, &quot;||&quot;)
print(r)  -- &quot;hello||world&quot;
</code></pre>
<ul>
<li>can concat only <code>part of the table</code></li>
</ul>
<pre><code class="language-lua">local t = {&quot;prefix&quot;, &quot;hello&quot;, &quot;world&quot;, &quot;postfix&quot;}
local r = table.concat(t, &quot;, &quot;, 2, 3)
print(r)  -- &quot;hello, world&quot;
</code></pre>
<h3 id="tablepack">table.pack</h3>
<ul>
<li>all arguments are packed into a table, and <code>n</code> field is added with the total number of arguments</li>
</ul>
<pre><code class="language-lua">local t = table.pack(&quot;hello&quot;, &quot;world&quot;, &quot;you&quot;, &quot;are&quot;, &quot;great&quot;)
print(type(t))  -- table
print(t.n)   -- 4

--[[
1       hello
2       world
3       you&#39;re
4       great
--]]
for i, v in ipairs(t) do
    print(i, v)
end
</code></pre>
<h3 id="tableunpack">table.unpack</h3>
<ul>
<li>unpack a table and return each separate element, can unpack a whole table or just a valid range of it</li>
</ul>
<pre><code class="language-lua">local t = {1, 2, 3, 4, 5}
print(table.unpack(t))  -- 1       2       3       4       5

local t = {&quot;hello&quot;, &quot;world&quot;, &quot;you&#39;re&quot;, &quot;great&quot;}
local n1, n2 = table.unpack(t, 2, 3)
print(n1, n2)  -- world   you&#39;re
</code></pre>
<h2 id="os-module">os module</h2>
<h3 id="ostime">os.time</h3>
<ul>
<li>current time if calling it without arguments</li>
</ul>
<pre><code class="language-lua">local now = os.time()
print(now)
</code></pre>
<ul>
<li>the time represented by the table argument</li>
</ul>
<pre><code class="language-lua">-- other fields will be ignored
local date_tab = {
    year = 2024,    -- Required
    month = 1,      -- Required (1-12)
    day = 15,       -- Required (1-31)
    hour = 14,      -- Optional (0-23)
    min = 30,       -- Optional (0-59)
    sec = 45,       -- Optional (0-61, for leap seconds)
    isdst = false   -- Optional (daylight saving time)
}

local timepoint = os.time(date_tab)
print(timepoint)
</code></pre>
<h3 id="osdate">os.date</h3>
<ul>
<li>current date string if calling it without any arguments</li>
</ul>
<pre><code class="language-lua">print(os.date())  -- Sun Dec 14 21:55:00 2025
</code></pre>
<ul>
<li>If format is <code>not *t</code>, then date returns the date as a string, formatted according to the same rules as the ISO C function <code>strftime</code></li>
<li>If format starts with ‘!’, then the date is formatted in UTC and if format is the string <code>*t</code>, then date returns a table with the following fields</li>
</ul>
<pre><code class="language-lua">local date_tab = os.date(&quot;!*t&quot;)
print(type(date_tab))  -- table
--[[
sec     11
yday    348
wday    1
min     3
month   12
hour    14
year    2025
isdst   false
day     14
--]]
for k, v in pairs(date_tab) do
    print(k, v)
end
</code></pre>
<h3 id="osexecute">os.execute</h3>
<ul>
<li>to test whether there is a shell, calling it without any arguments</li>
</ul>
<pre><code class="language-lua">local shell = os.execute()
print(shell)  -- true/false
</code></pre>
<ul>
<li>execute a command, just a wrapper of the <code>system</code> library function</li>
</ul>
<pre><code class="language-lua">local ok, what, code = os.execute(&quot;ls&quot;)
print(ok, what, code)  -- true  exit  0

-- if the command was terminated by a signal, then the what string will be `signal`
local ok, what, code = os.execute(&quot;hello&quot;)
print(ok, what, code)  -- nil  exit  127
</code></pre>
<h2 id="math-module">math module</h2>
<h3 id="mathpi">math.pi</h3>
<ul>
<li>if you need to get <code>pi</code>‘s value</li>
</ul>
<pre><code class="language-lua">print(math.pi)  -- 3.1415926535898
</code></pre>
<h3 id="mathhuge">math.huge</h3>
<ul>
<li>the float value HUGE_VAL, a value greater than any other numeric value</li>
</ul>
<pre><code class="language-lua">print(math.huge)  -- inf
</code></pre>
<h3 id="mathmaxinteger-and-mathmininteger">math.maxinteger and math.mininteger</h3>
<ul>
<li>current platform’s maximum and minimum integer values</li>
</ul>
<pre><code class="language-lua">print(math.maxinteger)  -- 9223372036854775807

print(math.mininteger)  -- -9223372036854775808
</code></pre>
<h3 id="mathmax-and-mathmin">math.max and math.min</h3>
<ul>
<li>as the name suggests, returning max and min value of arguments of each function. It would be better to combine with table.unpack function</li>
</ul>
<pre><code class="language-lua">local t = {2, 9, 1, 5, 7}

print(math.max(table.unpack(t)))  -- 9
print(math.min(table.unpack(t)))  -- 1
</code></pre>
<h3 id="mathrandomseed-and-mathrandom">math.randomseed and math.random</h3>
<ul>
<li>calling <code>math.randomseed</code> without arguments for <code>math.random</code> to generate random number each time</li>
</ul>
<pre><code class="language-lua">math.randomseed()
print(math.random())  -- result will be different each time math.random is called
</code></pre>
<ul>
<li>but calling <code>math.randomseed</code> with fixed arguments for <code>math.random</code> to generate the same number each time, this is good for <code>testing only</code></li>
</ul>
<pre><code class="language-lua">math.randomseed(1, 10)
print(math.random())  -- result will be the same each time math.random is called
</code></pre>
<h2 id="misc-stuff">misc stuff</h2>
<h3 id="z-escape-sequence"><code>\z</code> escape sequence</h3>
<p>It skips the following span of whitespace characters, including line breaks; it is particularly useful to break and indent a long literal string into multiple lines without adding the newlines and spaces into the string contents</p><pre><code class="language-lua">local s = &quot;This is a very long string that \z
can be broken into multiple lines in the source \z
code for readability without adding actual newlines \z
to the string&#39;s content.&quot;

print(s)

local s = &quot;\z     hello \z    world&quot;
print(s)  -- &#39;hello world&#39;
</code></pre>

            ]]>
        </content>
    </entry>
</feed>
